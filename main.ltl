// ltl-supervisor/main.ltl
//
// OTP-flavoured supervisor tree with restart strategies.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-supervisor v0.1.0")
    io::println("OTP-flavoured supervisor tree with restart strategies.")
    0
}
