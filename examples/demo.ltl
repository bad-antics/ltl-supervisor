// examples/demo.ltl — runnable demo for ltl_supervisor

import std::io
use ltl_supervisor as lib

fn main() {
    io::println("ltl_supervisor demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}